# alice-workbench · 语义文档（现在是什么）

> 语义文档纪律（AGENTS.md §5.20）：先写清「是什么」，实现逼近它，实践回修它。
> 本文描述**已实现**的契约；未实现项一律标 `未实现`，未验证项标 `待线上验收`。

## 1 · 元信息

| 项 | 值 |
|---|---|
| id | `alice-workbench` |
| 版本 | v0.2.0（星图图形化改版 · 2026-09-15） |
| 主副本 | 本文件（`projects/self/alice-workbench/docs/semantic.md`） |
| 关联 | 实施依据 `self-plugins/dsh-agent-cluster/docs/spec.md` §4.3 §4.5 §7-P1；表现层 `docs/DESIGN.md` |
| 最近复核 | 2026-09-15 |

## 2 · 定位与反定位

**定位**：常驻桌面的**观察与控制台**。把「本机有哪些智能体节点、它们此刻在做什么、DSH 运行时是否活着」变成一眼可见，且**零模型调用**（不烧 token）。

**反定位**（明确不做）：
- 不是 DSH 插件（不参与 profile 组合、不注入工具面）
- 不是消息路由器（不替主人转发、不做批处理判断——那是主脑的活）
- 不读 DSH 私有会话格式（`session.v3.jsonl.zstd`）——那会锁死未来（spec §4.5 修正 1）
- 不做智能决策（只看与只转发，判断永远归主脑/主人）

## 3 · 术语

| 词 | 含义 |
|---|---|
| 总线（bus） | 文件系统契约，默认 `%USERPROFILE%\.dsh-cluster`，可用 `DSH_CLUSTER_DIR` 覆盖 |
| 节点（node） | 可被寻址的智能体运行时：身份 + 能力 + 信箱 + 行为落盘 + 结果回传。**harness 无关** |
| 心跳 | `nodes/<nodeId>.json`，节点每 ~10s 覆写；`atMs` 是判活时间戳 |
| 行为事件 | `cluster-trace.jsonl` 一行一阶段（`phase`），是 R6「行为必须落盘」的现成载体 |
| 快照（snapshot） | 应用一次性读取的总线视图；`fingerprint` 是内容摘要，用于判断"变了没" |
| 星（star） | 节点在星图里的呈现；**位置由 `hash(nodeId)` 决定**（确定性径向布局，见 `docs/DESIGN.md` §4） |
| 卫星（satellite） | 任务在星图里的呈现，绕其 `assignee` 星运行；无 `assignee` 则绕中央恒星 |
| 光弧（link） | 消息的呈现（`from → to`；`owner` 解析为中央恒星）；最近 8s 加亮一次 |
| 脉冲（pulse） | 行为事件的图形化刻度（`cluster-trace` 的 `phase` + `logs/actions` 的 `stage`），点开才是全文 |

## 4 · 概念模型与不变量

```
Rust 后端（唯一写者=mailbox）
  bus::snapshot(dir)  ──读──▶  nodes/ · mailbox/ · state/ · cluster-trace.jsonl
        │
        └─ 指纹变化 ──▶ emit("bus-changed", Snapshot) ──▶ React 前端渲染
```

**不变量**：
- **I1 单一写面**：应用只写 `mailbox/<to>/<id>.json`，不改总线其它任何文件。
- **I2 只读判活**：节点在线判据 = `now - atMs < 30_000`，与插件 `DEFAULT_OFFLINE_AFTER_MS` 同源；应用**不猜测**节点状态。
- **I3 零模型调用**：应用不发起任何 LLM 请求（无 key、无 endpoint、无 SDK）。
- **I4 坏数据不致命**：任何单文件损坏（坏 JSON / 缺字段 / 超大）只跳过该文件，不影响其余视图（§5.24：逃逸异常 = 宿主死因）。
- **I5 harness 无关**：只依赖总线标准产物；`displayName`/`harness`/`capabilities` 等为**可选字段**，缺失时回退显示，不报错。

## 5 · 契约（含调用点清单）

### 5.1 Tauri IPC 命令（前端 → Rust）

| 命令 | 入参 | 出参 | 调用点（前端） |
|---|---|---|---|
| `bus_snapshot` | — | `Snapshot` | `src/App.tsx` 首帧 |
| `send_message` | `to`, `text`, `kind?` | 消息 id | `src/views/NodePanel.tsx` 提交 |
| `dsh_status` | — | `DshStatus` | `src/App.tsx`（10s 轮询） |
| `dsh_recover` | — | 启动说明 | `src/App.tsx` 顶栏按钮 |

### 5.2 后端事件（Rust → 前端）

| 事件 | 载荷 | 触发 |
|---|---|---|
| `bus-changed` | `Snapshot` | 内容指纹变化（150ms 轮询检测） |

### 5.3 总线读写契约

| 路径 | 权限 | 说明 |
|---|---|---|
| `nodes/*.json` | 只读 | 心跳；字段 `nodeId/atMs/pid/role/profile/workspace/port/...` |
| `mailbox/<node>/*.json` | 读 + **写** | 收件箱；写时 `v=1`、`from="owner"`、`ttlMs=86400000` |
| `mailbox/<node>/done/*.json` | 只读 | 已投递归档 |
| `state/*.json` | 只读 | 幂等集合 / 重试账（当前仅计入指纹） |
| `cluster-trace.jsonl` | 只读 | 行为事件流；取尾部 300 条 |

### 5.4 `from` 字段语义

工作台**代表主人**发话 ⇒ `from = "owner"`。它不冒充任何智能体节点（冒充会污染"谁说的"因果链）。

## 6 · 边界与信任

- **信任模型**：总线是**同机**协作面，非安全边界（对照 spec R1/R2）。应用不加认证，因为同一用户账号下任何进程本来就能读写这些文件。
- **越权防范**：`send_message` 校验目标名不含路径分隔符与通配符，防止写出 `mailbox/` 之外的路径。
- **隐私**：不落盘任何凭据；workspace 路径从心跳读取，**不硬编码**（也避免公开仓库泄露本机路径）。
- **副作用**：唯一外部可见副作用 = 写一条消息文件；`dsh_recover` 会**启动进程**（危险动作，UI 需二次确认——`未实现`，见 §10）。

## 7 · 可证伪验收

| # | 判据 | 状态 | 证据 |
|---|---|---|---|
| A1 | 打开应用即见 DSH 状态灯 + 节点名册 + 行为流（spec P1-1 前三项） | **已验证** | 真实 Tauri 窗口截图：HUD 四灯（演示数据/DSH 在线 :3080/节点 N 在线/总线已挂载）+ 左栏名册 + 右栏行为流 |
| **A1b** | **任务一句话列表**（spec P1-1 的末项） | **部分验证** | 数据层：`scan_tasks` 4 条单测（真实台账结构 / 坏文件跳过 / 未收尾优先排序 / 指纹联动）**16/16 绿**；UI：浏览器实机预览渲染正确（「1 未收尾 / 2 · 1 有待验证项」+ 两条一句话列表 + 未验证项高亮）。⚠ **真实总线数据 → Tauri 窗口这一跳我没能截图确认**（WebView2 内容截取在本环境不可靠）⇒ **待验收（归主人目视）** |
| **A1c** | **任务的「执行过程」可见**（节点在做什么，spec §5.2） | **部分验证** | 数据层：`scan_actions` 4 条单测（真实 6 行样本解析 / **taskId 回填到无 taskId 的 stage 行** / 坏行跳过 / 指纹联动）**16/16 绿**；UI：`TaskBoard` 展开显示分阶段时间线（`▸` start · `·` stage · `✓` done · `✕` failed，带时钟）。**诚实边界**：思考不落盘 ⇒ 只显示「在做什么」，不显示「在想什么」。⚠ 与 A1b 同：真实数据 → 窗口那一跳 **待验收（归主人目视）** |
| A2 | 点击节点展开其事件流并出现发消息入口（spec P1-2） | **已验证** | 点击后中栏显示：节点 id / 角色·harness / 心跳 / 进程·端口 / 工作区 + 相关事件 + 消息正文 + 底部 composer |
| A3 | DSH 未响应时一键恢复可调起运行时管理器，过程在行为流可见（spec P1-3） | **代码审查通过 / 实测受限** | 见下方「为什么没有实测 A3」 |
| A4 | 新增一个心跳文件后 **1s 内**节点出现在面板（spec P1-4） | **已验证** | 造非 DSH 探针心跳 → 截图在 +2s 内显示 `probe-p1-*` 于名册首位（0s 前）；实现为 150ms 指纹轮询，1s 判据有 6x 余量 |
| A5 | 应用自身零模型调用（spec P1-5） | **已验证（结构性）** | 无 LLM SDK / 无 endpoint / 无 key；后端只读本地文件 + 一次 TCP 探测 |
| A6 | 总线缺失/心跳损坏时应用仍可用 | **单元测试覆盖** | `snapshot_reports_missing_bus`、`scan_nodes_skips_corrupt_and_nameless`、`read_trace_skips_corrupt_lines_and_keeps_order` |
| A7 | Rust 单元测试全绿 | **已验证** | `cargo test` → **16 passed / 0 failed**（2026-09-15 复跑确认；含任务台账 4 条 + 行为事件 4 条） ✔ |
| **A8** | **星图为主视图**（打开即见天体，不是文字列表） | **已验证** | DOM 断言「默认进星图」：`svg.starmap` 在位、`g.star` > 2、且不出现「节点名册」 ✔ |
| **A9** | **布局确定性**（同一快照两次构建逐字节相同——星位不稳则主人失去「认星」能力） | **已验证** | 单测「确定性：同一输入两次构建逐字节相同」（`JSON.stringify` 比对） ✔ |
| **A10** | **卫星归属**：任务绕其 `assignee` 星运行；`assignee` 缺失或不存在 → 挂中央恒星 | **已验证** | 单测「卫星归属」（断言轨道半径 = 宿主半径 + `SAT_ORBIT_GAP`，含"assignee 不存在 → 中央"分支）+「坐标有限」 ✔ |
| **A11** | **文字纪律**：离线星不渲染名字标签 | **已验证** | 单测「环位与文字纪律」（离线星 `label === null`）+ DOM 断言「离线星不渲染 `text.label`、在线星有」 ✔ |
| **A12** | **点选即详情**：点星 → 节点详情（含发消息入口）；点卫星 → 任务详情（判据 / 裁决 / 执行过程齐备） | **已验证** | DOM 断言两条：`fireEvent.click(g.star)` 后出现「节点详情/节点 id/投递」；`fireEvent.click(g.sat)` 后出现「判据/裁决/执行过程」 ✔ |
| **A13** | **脉冲条**：默认折叠为刻度，点开才是完整事件流（文字退到第二层） | **已验证** | DOM 断言：折叠态无 `.pulse-full`；点开 `.pulse-toggle` 后 `.pulse-full` 出现且含「实时行为流」 ✔ |
| **A14** | **深链**：`#view=list` / `#task=<id>` / `#node=<id>` 决定初始视图与选中项 | **已验证** | DOM 断言「深链可直达」（`#task=` 打开即任务详情；点 `rect.map-bg` 回概览）+「切到列表」（列表态星图不渲染） ✔ |
| **A15** | **未验证项在星图上可见**（琥珀虚环——未验证不许被静默略过） | **已验证** | DOM 断言 `circle.unverified` ≥ 1（演示数据里那条 done-but-unverified 的任务） ✔ |
| **A16** | **全套回归** | **已验证** | `pnpm test` → **20 passed / 20**（布局 12 + 接线 8）；`tsc --noEmit` → 0 错；`pnpm build` → 43 modules；`cargo test` → **16 passed** ✔ |
| **A17** | 视觉终验（构图 / 配色 / 可读性）与「真实总线 → Tauri 窗口」那一跳 | **部分验证** | 我做了无头截图自查（`_tmp_review/starmap-v02b.png` + `crop-stars.png`：核心星徽标 0、probe 徽标 2、卫星对可辨、离线星在外环）并据此修了三处（画布比例 / 光弧对比 / 轨道半径）；WebView2 截取在本机不可靠 ⇒ **待验收（归主人目视）** |

### 为什么没有实测 A3（重要）

`dsh_recover` 会调 `<workspace>/.dsh/init-dsh.ps1`——而该脚本**第一步是清理既有 DSH 进程**。
本机此刻正运行着承载**当前会话**的 DSH 实例 ⇒ 实测一键恢复等于**杀死我自己**。
因此 A3 采用代码路径审查 + 受限说明：

| 审查项 | 结论 |
|---|---|
| 零硬编码 | ✓ workspace 从总线心跳读取，脚本路径由其推导 |
| 错误路径 | ✓ 无心跳 / 无脚本两种失败都有明确报错文案，不静默 |
| 不阻塞 UI | ✓ `spawn()` 不 `wait()`，立即返回 pid 与路径 |
| 不弹黑窗 | ✓ `CREATE_NO_WINDOW` |
| **危险动作二次确认** | ✓ 已实现（首次点击进确认态，5 秒内再点一次才执行；浏览器演示模式实测通过） |

**A3 的真实验收路径**（留给主人）：在 DSH 未运行时打开应用 → 点「一键恢复 DSH」→ 观察名册在数十秒内出现新心跳。


## 8 · 与实现关系

| 实现 | 位置 |
|---|---|
| 总线读取 / 指纹 / 轮询 / 事件 | `src-tauri/src/bus.rs` |
| DSH 探测与一键恢复 | `src-tauri/src/dsh.rs` |
| 命令注册与启动 | `src-tauri/src/lib.rs` · `main.rs` |
| 前端类型契约 | `src/types.ts`（与 Rust 结构体一一对应） |
| IPC 封装 | `src/api.ts` |
| 视图 | `src/views/Cockpit.tsx`（名册 + 任务板）· `NodePanel.tsx` · `ActionStream.tsx` · `TaskBoard.tsx`（任务一句话列表 + 点开看判据/证据/裁决） |
| 任务台账只读 | `src-tauri/src/bus.rs` 的 `scan_tasks`（数据源 `<busDir>/tasks/<taskId>.json`；协议主副本见 `dsh-agent-cluster/docs/semantic.md` §5.5） |
| 行为事件只读 | `src-tauri/src/bus.rs` 的 `scan_actions`（数据源 `<busDir>/logs/actions/<actionId>.jsonl`；把 `detail.taskId` 回填给同组每行，供「点开任务看执行过程」；协议主副本见 §5.7） |
| 验收探针（造非 DSH 心跳） | `scripts/mock-node.ps1`（`-Cleanup` 清理；**不是适配器**，只写心跳） |
| 星图布局（纯函数） | `src/starmap/layout.ts`（`buildStarMap` / `buildPulses` / `hashUnit`）——无 DOM、无随机、无 `Date.now()`，确定性可单测 |
| 星图渲染 | `src/views/StarMap.tsx`（SVG）+ `src/starmap/starmap.css` |
| 脉冲条 | `src/views/PulseBar.tsx`（刻度=相位着色，展开复用 `ActionStream`） |
| 上下文面板 | `src/views/DetailPanel.tsx`（概览 / 任务详情 / 节点详情三态；节点态复用 `NodePanel`） |
| 任务详情（单一副本） | `src/components/TaskDetail.tsx`——被上下文面板与列表视图的 `TaskBoard` **共用**（防两处渲染漂移） |
| 深链 | `src/App.tsx` 的 `readHash()`：`#view=list` · `#task=<taskId>` · `#node=<nodeId>`（让「某个任务」可被直接指出来，也给无头验收一条可复现入口） |
| 测试 | `src/starmap/layout.test.ts`（布局 12 条）+ `src/views/App.test.tsx`（视图接线 8 条，jsdom）；配置 `vitest.config.ts` |

## 9 · 实践修订记录

| 日期 | 修订 | 原因 |
|---|---|---|
| 2026-09-14 | **轮询替代 notify**（spec §4.3 原写「watch 总线目录（notify）」） | notify 在 Windows 上需第三方 crate + 已知怪异行为；150ms 轮询零依赖、可预测，对 1s 判据有 6x 余量。接口 `start_watch` 保持可替换 → 已回写 spec |
| 2026-09-14 | **详情层读「总线标准事件」而非 DSH 会话文件** | 执行 spec §4.5 修正 1：读 DSH 私有格式会锁死未来 harness 迁移 |
| 2026-09-14 | `from` 定为 `owner` | 工作台代表主人，不冒充节点（§5.4） |
| 2026-09-14 | **补 `capabilities/default.json`**（`core:event:allow-listen`） | Tauri v2 的权限系统默认**不**给前端事件监听权限；症状是**静默失效**——UI 照常显示首帧快照，只有实时推送没了（页面角落一条 unhandled rejection）。**「首帧能看」不等于「通道可用」** |
| 2026-09-14 | **错误展示分级**（启动期致命→全屏；运行时非致命→右下角提示条） | 最初把**所有** unhandled rejection 画成全屏覆盖层，结果一个非致命的权限拒绝把整个驾驶舱遮死。空白页与「盖住可用 UI」是两个极端，判据是**此时还有没有 UI 可看** |
| 2026-09-14 | **加 3s 轮询兜底** | 事件通道是快路（<1s），但权限/丢事件/窗口休眠都可能让它静默失效；两条路比一条路可靠 |
| 2026-09-14 | 前端 Tauri API 改**动态 import** | 顶层 import 让整个模块图在浏览器预览里求值（预览直接白屏）；改后构建产物出现独立 `core/event` chunk，浏览器预览与真窗口共用同一份代码 |
| 2026-09-14 | **托盘常驻落地**（部分关闭 U2） | 开启 `tray-icon` feature + 托盘菜单（显示工作台 / 退出）+ 主窗口 `CloseRequested` 拦截为 `hide()`。**通知能力刻意不自动化**：spec §4.3 规定通知仅在「三道门」通过时使用，自动提醒会绕过该纪律。判据可证伪：发 `WM_CLOSE` 后 `process alive = True` 且 `window visible = False` ⇒ 关闭 = 收进托盘 |
| 2026-09-14 | **窗口几何持久化落地**（关闭未决 U8） | 关闭（收托盘）与托盘退出时各保存一次 `outer_position()`/`inner_size()` 到 `<app_config_dir>/window.json`；启动时恢复，并**校验落点仍在某块显示器上**（拔掉外接屏后旧坐标会让窗口"消失"，比不恢复更糟——用 `available_monitors()` 判定）。<br>**踩坑（含一次方向性误判）**：本机 150% 缩放，Win32 `SetWindowPos(300,180)` 后 Tauri `outer_position()` 读数 450,270，比值正好 1.5。我据此断定"保存的是逻辑坐标"并把恢复改成 `LogicalPosition`——**方向错了**：真相是 **Win32 那一侧在互操作时按逻辑坐标走**，Tauri 侧自始至终是物理，正确配对就是 `PhysicalPosition`。教训：**不要用比值倒推单位归属，先确定是哪一侧的 API**。<br>**验证判据（往返自洽，可证伪）**：移动 → 关闭 → 重启 → 窗口位置不变（实测 Win32 视角 300,180 保持不变；默认位置本应居中，故非巧合） |

| 2026-09-14 | **P1-1 漏项补齐：任务视图** | spec §7-P1-1 原文含「**任务一句话列表**」，P1 交付时只落了状态灯/名册/行为流——**按自己的理解验收**导致整项漏掉（教训已入 `AGENTS.md` §5.9 规则 5：「通过」要看理由，验收逐字对照判据原文）。补齐：Rust `scan_tasks`（只读 `tasks/`，坏文件跳过，**未收尾优先排序**）+ `TaskBoard` 组件（一句话列表 + 点开看判据/证据/裁决，**未验证项必须显眼**）+ 指纹纳入 `tasks/`（否则列表停在旧状态）。数据源首次有真实内容：真实总线上的 `t-mu1gvjpy-d6sp9j`（由**参考适配器** ref-node 执行、主脑独立复现 sha256 后裁决 done） |

| 2026-09-15 | **执行过程可见（补 A1c）** | `TaskBoard` 的注释里写着「实时进度看 `logs/actions/`」，但工作台**其实读不到它**（只读 `cluster-trace.jsonl`）——**注释承诺了实现没有的东西**。补齐 `scan_actions`：读 `logs/actions/*.jsonl`，**把 `detail.taskId` 回填给同组每行**（否则 `stage` 行挂不上任务，前端无法按任务过滤），规模上限（24 文件 / 每文件 400 行 / 共 600 条），并纳入指纹（节点干活时界面必须动起来）。UI 在任务展开处渲染分阶段时间线。**教训**：注释里承诺的能力必须与实现对齐——否则下一个人（或下一个我）会以为它已经有了 |

| 2026-09-15 | **v0.2 星图图形化改版**（主人：「现在工作台字太多了，改成图形化界面，我偏好星图」） | 主区从「名册 + 任务板」改为**星图**（节点=星 / 任务=卫星 / 消息=光弧），行为流从整栏文字改为**底部脉冲条**（折叠成刻度、点开才是全文），长文本全部退到**右侧上下文面板**且必须点选才出现。**Rust 侧一字未改**——天体语义全部由现有快照字段推出（`role`/`assignee`/`from`/`to`/`status`），故后端无回归面。**保留 v0.1 全部已验证能力**：列表视图（名册 + 任务一句话列表）作为可切换回退视图，`TaskBoard` 原样保留、其详情渲染抽成 `TaskDetail` 单一副本。**新增判据**：布局确定性（同一快照两次构建逐字节相同——星位不稳则主人失去「认星」能力）、坐标有限性（NaN 会让星直接消失）、文字纪律（离线星不渲染名字标签）。**单测抓到的真缺陷**：光弧封顶原按「看过的消息」算，自环消息白占名额 ⇒ 改为按「画出来的弧」算 |

## 10 · 未决问题

| # | 问题 | 备注 |
|---|---|---|
| U1 | ~~`dsh_recover` 的二次确认~~ | ✅ **已实现**（2026-09-14，见 §9 实践修订记录） |
| U2 | ~~托盘常驻~~ ✅ **已实现** / 通知待做 | 托盘菜单（显示工作台 / 退出）+ 关闭即隐藏已落地并验证（`WM_CLOSE` 后进程存活、窗口隐藏）。**通知刻意不自动化**——spec §4.3 只在「三道门」通过时通知，接口位预留 |
| U3 | `displayName` 人话名规则 | 插件尚未上报（spec §3.4 扩展字段）；当前回退为 `web-0` 式短名 |
| U4 | ~~节点分组/关系图~~ ✅ **已实现（v0.2 星图）** | 星图即关系图（`docs/DESIGN.md` §4）。**节点分组**（按角色/能力分层）仍未做——当前只按在线/离线分内外环 |
| U5 | `cluster-trace.jsonl` 轮转 | 应用只读尾部 300 条；文件长期增长需要轮转策略（属插件侧） |
| U6 | 任务一句话列表的数据源 | spec §3.2 `tasks/` 台账尚未实现；MVP 显示事件流，无任务时为空态 |
| U7 | 权限面收口策略 | 当前给 `core:default` + `event:allow-listen`。将来加自定义命令权限时的策略未定（倾向：**能力全留 Rust 侧，前端只拿最小面**） |
| U8 | ~~窗口位置持久化~~ ✅ **已实现** | 关闭（收托盘）/ 托盘退出时保存几何，启动时恢复并校验落点仍在某块显示器内。往返自洽已实测（见 §9 实践修订记录）|
| U9 | 幽灵节点（有信箱、无心跳） | `mailbox/<node>/` 里存在但 `nodes/` 无心跳的节点，当前**不出现在星图上**（如 `primary`、`sim-node-a`）。要画出来需 Rust 侧补一个数据源（本轮刻意不动后端） |
| U10 | 视觉终验归主人 | 构图/配色/可读性我只做了**截图自查**（`_tmp_review/starmap-v02b.png`）；WebView2 内容截取在本机不可靠，故「真实总线 → Tauri 窗口」那一跳仍归主人目视 |
| U11 | 生产流程可视化（Factorio 范式） | `docs/DESIGN.md` §1 列了但未做：任务在天体上的流向（派发 → 执行 → 回结果 → 验收）目前只用卫星颜色与光弧近似表达 |
